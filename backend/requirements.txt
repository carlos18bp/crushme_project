--extra-index-url https://download.pytorch.org/whl/cpu

# Core web application
Django==5.2.17
django-cleanup==8.1.0
django-cors-headers==4.9.0
django-dbbackup==5.3.0
django-redis==6.0.0
django-silk==5.5.2
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
easy-thumbnails==2.10.1
huey==2.6.0

# Runtime and persistence
geoip2==5.3.0
gunicorn==23.0.0
mysqlclient==2.2.8
pillow==12.3.0
python-decouple==3.8
redis==7.1.0
requests==2.34.2

# Offline translation. Argos currently pins Stanza 1.10.1; settings.py forces
# MiniSBD so Stanza's vulnerable model loader is not reachable.
argostranslate==1.11.0
torch==2.13.0+cpu

# Fake-data commands used only in guarded non-production environments.
Faker==25.0.1

# Security pins for transitive dependencies.
PyJWT==2.13.0
setuptools==83.0.0
sqlparse==0.6.0
urllib3==2.7.0
